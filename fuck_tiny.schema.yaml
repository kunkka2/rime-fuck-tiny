# Rime schema settings
# encoding: utf-8

schema:
  schema_id: fuck_tiny
  name: Fuck Tiny
  version: "1"

engine:
  processors:
    - recognizer
    - key_binder
    - speller
    - punctuator
    - selector
    - navigator
    - express_editor
  segmentors:
    - ascii_segmentor
    - matcher
    - abc_segmentor
    - punct_segmentor
    - fallback_segmentor
  translators:
    - punct_translator
    - table_translator@custom_phrase
    - table_translator
    - reverse_lookup_translator
  filters:
    - simplifier
    - uniquifier

speller:
  alphabet: 'abcdefghjiklmnopqrstuvwxyz'
  delimiter: " `"
  max_code_length: 20
  auto_select: true

translator:
  dictionary: fuck_tiny
  enable_charset_filter: true
  enable_user_dict: true
  enable_completion: true
  enable_sentence: false

custom_phrase:
  dictionary: "fuck_tiny_userdict"
  user_dict: custom_phrase
  db_class: stabledb
  enable_sentence: true

reverse_lookup:
  dictionary: stroke_simp
  enable_completion: true
  prefix: "`"
  suffix: "'"
  tips: 〔笔画〕
  preedit_format:
    - xlit/hspnz/一丨丿丶乙/

punctuator:
  import_preset: default

key_binder:
  import_preset: default

recognizer:
  import_preset: default
  patterns:
    reverse_lookup: "`[a-z]*'?$"